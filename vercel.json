{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "corepack enable && pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @sofra/web build",
  "outputDirectory": "uygulamalar/web/.next",
  "regions": ["fra1"]
}
